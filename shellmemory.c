#include <stdio.h>
#include <stdlib.h>

// The file shellmemory.c contains the private data structures and public functions that implement the shell memory
struct MEM { char *var; char *value; };
struct MEM memArray[1000];

